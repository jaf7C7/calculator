{
	"name": "calculator",
	"version": "1.0.0",
	"description": "A simple calculator capable of four incredible operations.",
	"main": "calculator.js",
	"type": "module",
	"scripts": {
		"test": ". ./.env && node --test",
		"lint": ". ./.env && npx eslint",
		"format": ". ./.env && npx prettier --write ."
	},
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"@eslint/js": "^9.17.0",
		"eslint": "^9.17.0",
		"globals": "^15.14.0"
	}
}
